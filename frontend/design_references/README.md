# Design References

This folder is used to store reference pictures, UI mockups, and inspiration images for the LegacyLens frontend (like the landing page). 

You can drop your image files (`.png`, `.jpg`, `.fig`, etc.) directly into this folder.
